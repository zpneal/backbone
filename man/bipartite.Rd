% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.helpers.R
\name{bipartite}
\alias{bipartite}
\title{bipartite: generates a backbone object where rows and columns are}
\usage{
bipartite(B, rows = TRUE, cols = TRUE, trials = NULL)
}
\arguments{
\item{B}{graph: Bipartite graph object of class matrix, sparse matrix, igraph, edgelist, or network object.}

\item{rows}{boolean: TRUE if the model should constrain the row sums, FALSE if not.}

\item{cols}{boolean: TRUE if the model should constrain the column sums, FALSE if not.}

\item{trials}{integer: number of trials to be ran in the case where fdsm is used (rows = TRUE, cols = TRUE)}
}
\value{
backbone, a list(positive, negative, summary). Here
    `positive` is a matrix of proportion of times each entry of the projected matrix B is above the corresponding entry in the generated projection,
    `negative` is a matrix of proportion of times each entry of the projected matrix B is below the corresponding entry in the generated projection,
    `summary` is a data frame summary of the inputted matrix and the model used including: model name, number of rows, skew of row sums, number of columns, skew of column sums, and running time.
}
\description{
bipartite: generates a backbone object where rows and columns are
}
\examples{
bipartite(davis, rows = TRUE, cols = FALSE) #runs hyperg on davis data
}
